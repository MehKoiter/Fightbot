# Railway Deployment Configuration for FightBot
# Optimized for Discord bot deployment

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run debug-env"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[env]
NODE_ENV = "production"
